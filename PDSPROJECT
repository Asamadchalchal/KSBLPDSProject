Spyder Editor

This is a temporary script file.
"""

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_customers_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_geolocation_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_order_items_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_order_payments_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))


# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_order_reviews_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_orders_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_products_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))


# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/olist_sellers_dataset.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))

# import pandas as pd

# path = r"C:/Users/Hp/Desktop/pds project work files/product_category_name_translation.csv"

# data = pd.read_csv(path)

# df = data.dropna(axis = 0, how = 'any')

# print ("Old data frame length:", len(data), "\nNew data frame length:", len(df), "\nNumber of rows with atleast 1 NA value: ", (len(data)-len(df)))
